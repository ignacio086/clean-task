interface Task{
    id: number,
    title: string,
    description: string,
    done: boolean
}
interface TaskServer {
    id: number;
    title: string;
    description: string;
    done: boolean;
  }